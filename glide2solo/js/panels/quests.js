// Panel: Tracker questów (Guild Jobs + Odd-Jobs). Zaimplementowane w Fazie 6.
export function render(root) {
    root.innerHTML = `<p class="placeholder">Tracker questów — w budowie (Faza 6).</p>`;
}
