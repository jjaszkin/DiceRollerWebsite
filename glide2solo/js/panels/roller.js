// Panel: Uniwersalny roller (wszystkie tabele podręcznika). Zaimplementowane w Fazach 3-5.
export function render(root) {
    root.innerHTML = `<p class="placeholder">Roller — w budowie (Fazy 3-5).</p>`;
}
