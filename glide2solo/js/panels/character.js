// Panel: Karta postaci (Seeker) + tracker zasobów. Zaimplementowane w Fazie 2.
export function render(root) {
    root.innerHTML = `<p class="placeholder">Karta postaci — w budowie (Faza 2).</p>`;
}
